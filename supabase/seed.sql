-- seed.sql — date demo pentru dev. Rulat de `supabase db reset`.
--
-- NOTĂ: userii auth NU se pot semăna portabil aici (parole/hash-uri gestionate
-- de GoTrue). Flow-ul de seed pentru dev: creezi userii prin UI (/signup) sau
-- `supabase auth admin` API, apoi rulezi probele din docs/VERIFICATION.md.
-- Tabelele de conținut (tours, days, …) primesc seed demo începând cu Faza 1.

select 1;
